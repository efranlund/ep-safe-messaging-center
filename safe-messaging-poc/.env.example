# NextAuth Configuration
# Required: Set this to your production URL (e.g., https://your-app.vercel.app)
NEXTAUTH_URL=http://localhost:3000

# Required: Generate a secure random string for production
# You can generate one with: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-change-this-in-production

# Google OAuth (Required for Google sign-in)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Email Provider (Required for magic link authentication)
# Format: smtp://username:password@smtp.example.com:587
# For production, use a service like SendGrid, AWS SES, or Resend
EMAIL_SERVER=smtp://user:pass@localhost:1025
EMAIL_FROM=noreply@eliteprospects.com

